model:
  name: dummy-model

profiles:
  throughput:
    max_batch_size: 8
    max_concurrent_requests: 10

  latency:
    max_batch_size: 1
    max_concurrent_requests: 2

  balanced:
    max_batch_size: 4
    max_concurrent_requests: 5
